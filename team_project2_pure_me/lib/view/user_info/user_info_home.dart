import 'package:flutter/material.dart';

class UserInfoHome extends StatelessWidget {
  const UserInfoHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // 자기 게시글의 사진들만 List로 가져오는 함수
    );
  }
}