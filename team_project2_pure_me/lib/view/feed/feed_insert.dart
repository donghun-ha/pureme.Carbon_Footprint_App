import 'package:flutter/material.dart';

class FeedInsert extends StatelessWidget {
  const FeedInsert({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // imagePicker
      // 피드 입력하는 함수
    );
  }
}