import 'package:flutter/material.dart';

class CalcElec extends StatelessWidget {
  const CalcElec({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // 텍스트필드의 내용을 insertCarbonGen(String kind, String amount)에 넣어준다.
    );
  }
}