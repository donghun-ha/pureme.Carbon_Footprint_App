import 'package:flutter/material.dart';

class CalcFood extends StatelessWidget {
  const CalcFood({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // 계산하는 함수, 집어넣어주는 함수
    );
  }
}