import 'package:flutter/material.dart';

class RankPage extends StatelessWidget {
  const RankPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // 유저의 정보를 10등까지 저장할 리스트 rankList
      ///// 각각의 유저의 랭크는 1~10임.
      // 내 rank를 끌어올 변수 myrank
      // rankList를 불러오는 함수 : fetchRank
      // myrank를 불러오는 함수 : fetchMyRank
    );
  }
}